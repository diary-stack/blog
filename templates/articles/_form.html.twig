{{ form_start(form) }}
<div class="container-fluid">
  <div class="row">
    <div class="col-lg-6 offset-lg-3">
      {{ form_widget(form) }}
      <button class="btn btn-primary form-control my-2">
        {{ button_label|default('Save') }}
      </button>
    </div>
  </div>
</div>
{{ form_end(form) }}
