<div class="navigation position-relative">
	<nav class="container d-flex justify-content-between pt-3 align-items-center">
		<div>
			<a href="{{path("app_home")}}" class="logo">
				<b>
					REVIEW
					<br>
					EMPIRE
					<sup class="sup">TM</sup>
				</b>
			</a>
		</div>
		<div class="d-none d-lg-block">
			<ul class="m-auto">
				<li>
					<a href="{{ path("app_home" )}}">Home</a>
				</li>
				<li>
					<a href="{{ path("app_articles" )}}">Articles</a>
				</li>
				<li>
					<a href="{{ path("app_about" )}}">About</a>
				</li>
				<li>
					<a href="{{ path("app_contact" )}}">Contact</a>
				</li>
			</ul>
		</div>
		<div class="d-flex">
			{% if app.user %}
				<a href="{{ path("app_profil") }}" class="custom-btn">Profil</a>
				{% if 'ROLE_ADMIN' in app.user.roles %}
					<a href="{{ path("app_articles_index") }}" class="ms-1 custom-btn">Admin</a>
				{% endif %}
			{% else %}
				<a href="{{ path("app_login") }}" class="custom-btn">Login</a>
				<a href="{{ path("app_register") }}" class="ms-1 custom-btn">Register</a>
			{% endif %}
			<button class="bars-btn btn ms-1 border-1 border d-block d-lg-none">
				<i class="fas fa-bars"></i>
			</button>
		</div>
	</nav>
	<div class="position-absolute navbar-bars w-100 ">
		<ul class="d-flex flex-column align-items-center p-0">
			<li class="mt-4 mb-3">
				<a href="{{ path("app_home" )}}">Home</a>
			</li>
			<li class="mb-3">
				<a href="{{ path("app_articles" )}}">Articles</a>
			</li>
			<li class="mb-3">
				<a href="{{ path("app_about" )}}">About</a>
			</li>
			<li class="mb-3">
				<a href="{{ path("app_contact" )}}">Contact</a>
			</li>
		</ul>
		<div class="position-absolute navbar-bars-close top-0 end-0 m-3">
			<i class="fas fa-close"></i>
		</div>
	</div>
</div>
