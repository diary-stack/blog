{% extends 'base.html.twig' %} {% block title %}New Contacts {% endblock %} {%
block body %}
<div class="container mt-5">
  <div class="row">
    <div class="col-lg-6 offset-lg-3 px-5">
      <h1 class="text-center">Get in Touch</h1>
      <p class="text-center">
        Reach Out Us for Questions, Collaborations, or Just to Say Hello
      </p>
      {% if success %}
      {{ success }}
      {% endif %}
      {{ include("contact/_form.html.twig") }}
    </div>
  </div>
</div>

{% endblock %}
