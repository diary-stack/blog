{% extends 'admin.base.html.twig' %} {% block title %}Contacts {% endblock %} {%
block body %}
<h1>Contacts</h1>

<table class="table">
  <tbody>
    <tr>
      <th>Id</th>
      <td>{{ contact.id }}</td>
    </tr>
    <tr>
      <th>Fullname</th>
      <td>{{ contact.fullname }}</td>
    </tr>
    <tr>
      <th>Subject</th>
      <td>{{ contact.subject }}</td>
    </tr>
    <tr>
      <th>Email</th>
      <td>{{ contact.email }}</td>
    </tr>
    <tr>
      <th>Massage</th>
      <td>{{ contact.massage }}</td>
    </tr>
  </tbody>
</table>

{{ include("contact/_delete_form.html.twig") }}
{% endblock %}
